[build]
  publish = "."
  command = "echo 'Frontend build completed - Static files ready'"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[dev]
  command = "echo 'Dev server ready'"
  port = 3000
  publish = "."
  framework = "#static"
